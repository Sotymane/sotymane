import styles from "./Header.module.css";

export const Header = () => {
    return (
        <header className={styles.header}>
            <h1>SOTYMANE</h1>
            <h2>MUSIC PRODUCER & AUTHOR</h2>
        </header>
    );
};
